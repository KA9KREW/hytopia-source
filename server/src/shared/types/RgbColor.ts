/**
 * An RGB color. `r`, `g`, and `b` expect a value between 0 and 255.
 *
 * **Category:** Types
 * @public
 */
interface RgbColor {
  r: number;
  g: number;
  b: number;
}

export default RgbColor;
